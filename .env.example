# Origin of the Relearn backend (RelearnCorp/Backend-repository).
# Both apps are Next.js on port 3000 by default — run the backend on another
# port, e.g. `PORT=3001 pnpm dev`, and point this at it.
NEXT_PUBLIC_API_URL=http://localhost:3001
JWT_SECRET=yoursecretkey